const notas = [6.7,8.8, 9.8, 8.1 ]

for (let i in notas) {
console.log(i)
}

const pessoas = {
    nome: 'Ana',
    sobrenome: 'Pinheiro',
    peso: 90,
    idade: 50
}
for(let atributo in pessoas){
    console.log($(atributo)=$(pessoa [atributo]))
}
